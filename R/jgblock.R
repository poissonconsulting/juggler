is.jgblock <- function(x) {
  inherits(x, "jgblock")
}

jgblock <- function(x) {
  x <- list()
  class(x) <- "jgblock"
  x
}

names.jgblock <- function(x) {
  names(x)
}

names.jgblock <- function(x) {
  names(x)
}
